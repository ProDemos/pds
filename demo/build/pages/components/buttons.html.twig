{% extends 'styleguide/component.html.twig' %}
{% import 'macros/components.html.twig' as ui %}


{% block head_title 'Buttons - Styleguide' %}


{% block component_intro %}
   <h1 class="c-copy--h1">Buttons</h1>
    <p>List of all kind of buttons used throughout the project. Every button starts with a class (c-btn) that contains the shared behaviour. 
            There are modifiers to describe the primary or secondary style, and the themes, when used, primarily consists of colors for the default, hover, and focus states. If you want a button to be present but not yet clickable, just add the disabled modifier class provided in the example.
            The buttons with an icon contain an extra container with the SVG-code. Just choose a button that fits best to your situation, copy the code and alter to your needs.
    </p>
    <div class="s-default">
        <h4>Elements and modifiers</h4>
        <ul>
            <li> .c-btn[ .primary | .secondary ]</li>
            <li> .c-btn[ .large | .regular |.small ]</li>
            <li> .c-btn[ .accent | .disabled ]</li>
            <li> .c-btn[ .toggle | .transparent ]</li>
            <li> .c-btn > c.btn-label</li>
            <li> .c-btn > .c-btn-content > [ .c-btn-icon.left + ] .c-btn-label[ .left | .right ]  [ + .c-btn-icon.right ] </li>
        </ul>
    </div>

{% endblock %}

{% block component_list %}
    {% for component in components.buttons %}
        {% if component.icon %}
            {% set icon = component.icon|merge({'directory': rooturi ~ 'assets/images/icons' })  %}
            {% set component = component|merge({'icon': icon}) %}
        {% endif %}
        {% if component.icon_right %}
            {% set icon_right = component.icon_right|merge({'directory': rooturi ~ 'assets/images/icons' })  %}
            {% set component = component|merge({'icon_right': icon_right}) %}
        {% endif %}
        {% if component.icon_left %}
            {% set icon_left = component.icon_left|merge({'directory': rooturi ~ 'assets/images/icons' })  %}
            {% set component = component|merge({'icon_left': icon_left}) %}
        {% endif %}
        {{ ui.styleguide_component(component.styleguide_type, component, styleguide.outputs) }}
    {% endfor %}
{% endblock %}
