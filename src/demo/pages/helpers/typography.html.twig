{% extends 'base.html.twig' %}

{% block head_title 'Typography' %}


{% block body_intro %}

    <h1 >Typography</h1>

    <h2 >Function: pds.pds-fontsize</h2>
    <p>
        <b><code>pds.pds-fontsize(name)</code></b>
        <dl>
            <dt><code>name</code></dt>
            <dd>refers to a fontsize name, like 'huge'</dt>
        </dl>

    <hr>
        Example:
<xmp>
@use "..path-to-pds/sass/pds";

h1 {
    font-size: pds.pds-fontsize(huge);
}
</xmp>
    
    <hr>

        Font sizes are defined as a map, like 
<xmp>
$pds-fontsizes: (
  none: 0,
  tiny: 8px,
  xxs: 12px,
  xs: 14px,
  s: 16px,
  default: 18px,
  m: 22px,
  l: 24px,
  xl: 32px,
  xxl: 44px,
  huge: 56px,
);
</xmp>
        You can override these font sizes, or define your own,
        when configuring settings:
<xmp>
use "...path-to-pds/sass/settings" with ( 
    $pds-assets-path: "...path-to-pds",
    $pds-custom-fontsizes: (
        header-large: 4rem,
        footnote: .5rem
    )
);
...
@use "...path-to-pds/sass/pds";
h1 {
    font-size: pds.pds-fontsize(header-large);
}

</xmp>
    </p>

    <h2 >Function: pds.pds-lineheight</h2>
    <p>
        <b><code>pds.pds-lineheight(name)</code></b>
        <dl>
            <dt><code>name</code></dt>
            <dd>refers to a lineheight name, like 'default'</dt>
        </dl>

    <hr>
        Example:
<xmp>
@use "...path-to-pds/sass/pds";

h1 {
    line-height: pds.pds-lineheight(default);
}
</xmp>
    
    <hr>

        Line heights are defined as a map, like 
<xmp>
$pds-lineheights: (
  none: 0,
  tiny: 1,
  xxs: 1,
  xs: 1,
  s: 1.25,
  default: 1.5,
  m: 1.75,
  l: 1.75,
  xl: 1.75,
  xxl: 1.75,
  huge: 1.75
);
</xmp>
        You can override these lineheights, or add your own, when
        configuring settings.scss:
<xmp>
use "...path-to-pds/sass/settings" with ( 
    $pds-assets-path: "...path-to-pds",
    $pds-custom-lineheights: (
        header-large: 1.2,
        footnote: .5
    )
);
...
@use "...path-to-pds/sass/pds";

h1 {
    line-height: pds.pds-lineheight(header-large);
}

</xmp>

    </p>

    <h2 >Mixin: pds.pds-slant</h2>
    <p>
        <b><code>pds.pds-slant([color], [bottom-left|bottom-right], [size])</code></b>
        <dl>
            <dt><code>color</code> (default blue)</dt>
            <dd>refers to a color name, like 'blue-40'</dt>
        </dl>
        <dl>
            <dt><code>bottom-left|bottom-right</code> (default bottom-left)</dt>
            <dd>indicates the direction of the slant</dt>
        </dl>
        <dl>
            <dt><code>size</code> (default 10px)</dt>
            <dd>the size of the slant</dt>
        </dl>

    <hr>
        Example:
<xmp>
@use "...path-to-pds/sass/pds";
.infobox {
    @include pds.pds-slant(red)
}
</xmp>
    
    
    </p>

{% endblock %}

