{% extends 'buttons/_button.html.twig' %}
{% import 'macros.twig' as ui %}

{% block button_content %}
    {% if bem %}
        <span class="c-button__icon-container">
            <span class="c-button__icon-container__label  c-button__icon-container__label--left">
                {% if text %}
                    {{ text }}
                {% endif %}
            </span>

            <!-- replace the SVG-code with the appropriate from the folder 'images/icons/24px' -->
            {{ ui.svg(icon.name, {
                width: icon.width|default(10),
                height: icon.height|default(10),
                directory: icon.directory|default('assets/images/icons/24px'),
                css_classes: ['c-btn-icon', 'right', 'icon--' ~ icon.name|basename]
            }) }}
        </span>
    {% else %}
        <span class="c-btn-container">
            <span class="c-btn-label left">
                {% if text %}
                    {{ text }}
                {% endif %}
            </span>

            <!-- replace the SVG-code with the appropriate from the folder 'images/icons/24px' -->
            {{ ui.svg(icon.name, {
                width: icon.width|default(10),
                height: icon.height|default(10),
                directory: icon.directory|default('assets/images/icons/24px'),
                css_classes: ['c-btn-icon', 'right', 'icon--' ~ icon.name|basename]
            }) }}
        </span>
    {% endif %}    
{% endblock %}
