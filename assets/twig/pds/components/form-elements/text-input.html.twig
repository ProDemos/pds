{% set cx = html_classes({
    'invalid': invalid,
    'valid': valid,
    'disabled': disabled,
    't-corporate': theme == 'corporate',
    't-theme2': theme == 'theme2',
    't-theme3': theme == 'theme3',
    't-theme4': theme == 'theme4',
    't-theme5': theme == 'theme5',
    't-theme6': theme == 'theme6',
    't-theme7': theme == 'theme7',
}) %}
<input class="pds-c-input text {{ cx }}" id="{{ id }}" name="{{ name }}" type="{{ type }}" value="{{ value }}" placeholder="{{ placeholder }}" {{ input_attributes }}>
